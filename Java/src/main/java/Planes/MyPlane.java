package Planes;

public class MyPlane  extends Plane {
    public MyPlane(String modelPlane, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(modelPlane, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}